import AppScreen from "../../../components/AppScreen";

export default function SharedContentScreen() {
  return <AppScreen title="Shared Tracks/Posts" subtitle="Music and posts shared inside conversations." />;
}
